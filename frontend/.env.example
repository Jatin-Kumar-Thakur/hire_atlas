# API base URL
# Leave blank in development — the Vite proxy forwards /api → http://localhost:5000/api
# In production, set this to your deployed backend URL, e.g. https://api.yourdomain.com/api
VITE_API_URL=
